# Authlode SSO 用户管理平台 — 功能目标与实现方案（总体研判）

> **版本**: v4.1（2026-09-19）
> **v4.1 变更**: 术语校准（租户 = 接入的系统）、一套账号体系原则（两级门禁）、访问控制执行点已决（组织 Token 强制约定）、客户应用接入拓扑（3.2b）、自助中心升为路线图内（批次 4 核心）、与 IAM 面板关系（1.5 分层而非替代）、产品形态与用户面（1.6）
> **性质**: 当前权威总纲 — 定位、功能目标、实现方案的整体研判
> **文档关系**: 本文统领定位与规划；[总体技术方案.md](总体技术方案.md) 提供架构细节；[功能模块集成与实现规划.md](功能模块集成与实现规划.md) 提供模块级集成/实现拆分与 Logto 调研结论。**冲突时以本文为准**。

---

## 一、定位研判

### 1.1 定位演进

| 版本 | 定位 | 评价 |
|------|------|------|
| v1 (2026-03) | Logto 专用用户中心 | 耦合单一平台，无多租户 |
| v2 (2026-09-18) | 多租户 IAM 用户管理系统 | 引入多租户 + Provider 抽象，但"IAM 管理"偏泛 |
| **v3 (本文)** | **SSO 用户管理平台** | 品类清晰、价值聚焦 |

### 1.2 最终定位陈述

> **Authlode 是一个 SSO 用户管理平台**：以 IAM 为身份底座（Logto 为第一实现平台），面向多租户提供**用户全生命周期管理**与 **SSO 应用接入管理**。租户管理员在平台上自助管理用户、组织、权限，并接入应用；用户**一次登录，通行租户内所有已接入应用与平台本身**。

> **术语约定（2026-09-19 校准）**: 本平台的"**租户**" = **接入的系统**（业务系统/应用），而非客户公司。多租户 = 多系统：**账号体系统一**（全局用户池，同 email 同账号，一份凭证走遍所有系统）、**系统间隔离**（各系统只见自己的用户）、一个用户可用多个系统（多组织归属，各系统独立角色）。接入一个系统 = 建一个 Organization（用户分组+隔离容器）+ 注册其 Application（OIDC 认证入口）。本平台自身也是该账号体系的消费者（登录后才能操作）。
>
> **一套账号体系原则（2026-09-19 确认）**: 类比 Google 账号体系 — 账号池**唯一且全局**，系统只是账号的**访问范围**而非独立账户体系（无人能自建账户体系）；**两级门禁**: ① 有账号?（未注册则认证本身失败） ② 有资格?（非系统成员无法获得该系统的访问凭证，由组织 Token 强制）。账号因系统邀请而诞生（双语义），不存在独立于系统的公开注册。

### 1.3 为什么这个定位更准

1. **品类可识别**: "SSO 用户管理平台"是成熟产品品类（Okta/OneLogin 一类），比"IAM 用户管理系统"（过泛）与"Logto 用户中心"（过窄）都准确
2. **价值主张聚焦**: 应用接入（SSO）从"模块之一"升为**产品核心价值** — 租户来平台是为了管用户的 SSO 访问
3. **多租户是架构不是叙事**: 软隔离是实现方式，产品故事是"每个租户自己的用户与 SSO 管理"
4. **通用性保留**: IAM 无关（Provider 抽象不变），Logto 只是第一实现

### 1.4 定位不变项（前期决策全部维持）

- 多租户软隔离: 单 Logto 实例，Organization = 租户（官方文档模式，调研已证实）
- 租户自管理 + 运营方预置（无平台端、无自助开通）
- 管理界面分工: 实例管理 = Logto 后台，租户管理 = 本平台
- 数据分层: 身份在 IAM，自有数据带 tenantId 强制隔离
- 邀请双语义 / 租户管理员 = org role / Tenant 记录 JIT 创建

### 1.5 与 IAM 面板的关系 — 分层而非替代

常见疑问:"我们是不是替代了 IAM 自己的管理面板?" 准确回答是**分层**:

| 层 | 谁提供 | 内容 |
|----|--------|------|
| 认证引擎 | IAM (Logto) | 登录页、OIDC 协议、Token/会话、连接器 — **不替代** |
| 实例管理面板 | IAM Admin Console（运营方用） | 实例配置、连接器、开通租户 — **不替代**（管理界面分工决策） |
| **租户产品层** | **Authlode（本平台）** | 多租户隔离、租户自助管理、邀请注册、SSO 应用接入策略、租户级审计 — **IAM 不提供，产品价值所在** |

与 IAM 面板重叠的用户/角色/应用 CRUD 只是入场券（租户作用域的同类操作）。Logto 面板是单租户、运营方专用、无自助概念——官方定位即"基础设施，产品层用 API 自建"。**本平台是 IAM 之上的多租户产品层，不是面板的克隆。**

### 1.6 产品形态与用户面（"用户中心"的准确理解）

常见类比:"我们做的是 member.xxx.xx 这种用户中心吧?" 方向对,三点校准:

1. **是"用户中心的工厂"而非单个用户中心**: 经典 member.xxx.xx 是单公司统一登录;本平台是多租户的 — 每个租户通过我们获得"他们自己的用户中心"(用户/组织/SSO 应用接入),租户间互不可见
2. **登录页不是我们的**: 登录 UI 由 IAM (Logto) 托管(支持租户级品牌化 T1.5),本平台不做登录页 — "IAM 层保持薄"的又一体现
3. **终端用户的 member 式体验是可选增强而非核心**: 核心产品是管理员控制台

| 产品面 | 谁用 | 状态 |
|--------|------|------|
| **租户管理控制台**（核心产品） | 租户管理员 | 批次 1-3 交付 |
| 公开注册页（邀请接受,免登录） | 受邀用户 | 批次 3 |
| 终端用户自助中心（"我的应用"门户） | 租户成员 | **路线图内（批次 4 核心）** — 统一账号体验的必要组成 |
| 登录页 | 所有用户 | **Logto 托管**,非本平台交付物 |

> 命名兼容: "用户中心"是租户视角的体验描述（每个租户得到一个用户中心）,"SSO 用户管理平台"是产品品类定位 — 对外可并用:"多租户 SSO 用户管理平台,让每个租户拥有自己的用户中心"。

**一句话定位（前厅/后厨表述）**: **Logto 是用户中心的引擎（后厨）,Authlode 是用户中心本身（前厅+产品层）**。管理台在 Authlode、用户面在 Authlode、业务规则（隔离/策略/邀请/审计）在 Authlode;引擎归 Logto、登录页归 Logto（可按系统品牌化）、实例管理归运营方（Logto 后台）。注意: Authlode 不是 Logto 的"一层皮" — 拥有自己的业务后端与数据域,这是产品价值所在。

---

## 二、功能目标研判

### 2.1 端到端价值主线（产品的一句话）

```
运营方开通租户 → 管理员登录平台 → 邀请/管理用户 → 接入应用
→ 用户一次登录, 通行平台 + 租户所有已接入应用
```

### 2.2 核心目标（产品必须交付）

| # | 目标 | 内容 | 对应模块 |
|---|------|------|---------|
| G1 | 认证与租户上下文 | OIDC 登录、TenantContext、租户切换器 | 认证授权 |
| G2 | 用户管理 | 生命周期（查/建/改/停/删）、搜索分页、扩展信息 | 用户模块 |
| G3 | 邀请注册 | 双语义（新 email 建号 / 已有 email 入租户） | 邀请模块 |
| G4 | 组织与权限 | 租户内组织树、RBAC、管理员任命 | 组织/角色模块 |
| G5 | **SSO 应用接入（核心价值）** | 租户接入应用（OIDC client）、用户-应用访问控制、接入配置 | 应用接入模块 |
| G6 | 安全与审计 | MFA（租户级）、审计日志、会话管理 | 审计/设置模块 |

### 2.3 支撑目标

批量操作、系统通知、数据统计、系统设置（租户级）

### 2.4 机会目标（SSO 定位下优先级调整）

| 机会 | 调整 | 理由 |
|------|------|------|
| T1.1 企业 SSO 入站（BYO IdP） | ⬆ 上调（批次 4 可选） | SSO 平台的自然延伸：租户员工用自己公司 IdP 登录（账号仍落统一池、仅认证联邦化，与一套账号体系原则兼容） |
| T1.4 组织 Token / 组织级 API 资源 | ⬆ 上调 | 租户应用获得租户作用域授权的机制（与 G5 协同） |
| T2.1 终端用户自助中心 | ⬆⬆ 升为路线图内（批次 4 核心） | 统一账号体验的必要组成：用户登录后操作自己的账号（改密/资料/我的系统/我的应用） |
| T2.3 SCIM 自建 | 维持 | 差异化，P1 后可做 |
| 其余 T1/T2/T3 | 维持 | 见功能模块规划第九节 |

### 2.5 明确不做

- 平台端 / 租户自助开通（运营方预置）
- IAM 实例管理（Logto 后台职责）
- 硬隔离多实例（远期再议）

---

## 三、实现方案研判

### 3.1 架构层 — 全部维持（研判通过）

| 架构决策 | 结论 | 依据 |
|----------|------|------|
| 单实例 + Organization = 租户 | ✅ 维持 | Logto 官方多租户 SaaS 文档模式 |
| IAM Provider 抽象 + Logto Adapter | ✅ 维持 | 定位通用性的基础 |
| 认证归 IAM（OIDC）/ 授权归平台（TenantContext） | ✅ 维持 | split-brain 已显式设计 |
| 数据分层 + tenantId 强制过滤 | ✅ 维持 | 隔离机制 |
| 多组织归属 / org 角色差异化 / 建号带密码 | ✅ 可行 | 调研证实 Logto 原生支持 |

> **Provider 抽象的现实边界**: "后端接什么 IAM 都差不多"是**设计目标而非免费午餐**。能力差异真实存在——仅 Logto 就有: org 角色模板级、交互应用实例级、无 SCIM; 其他 IAM 的"租户"映射单位各不相同（Keycloak realm / Auth0 organization / Authentik 无原生组织）。两条实现纪律: ① 业务代码零 IAM 概念泄漏（Provider 重构列 P0 的原因）② 能力差异经 `getCapabilities()` 显式暴露，功能按 IAM 支持度降级。**产品的护城河不在 IAM 对接层，在多租户产品层与扩展数据域——IAM 层保持薄。**

### 3.2 SSO 核心机制（本定位下的关键阐明）

**单 IdP 会话共享**: 平台自身与租户接入的应用都走**同一个 Logto OIDC 端点**认证。用户登录一次（Logto 建立会话）→ 访问平台或任何已接入应用时免再次登录。这是"一次登录通行"的机制基础，**无需额外开发**，是选择"单实例软隔离"架构的直接红利。

### 3.2b 客户应用接入拓扑（要接几套系统？）

**每个角色只见一套系统，核心认证链路上本平台零依赖**:

| 角色 | 接入谁 | 接什么 |
|------|--------|--------|
| **客户应用**（运行时） | **仅 Logto** | 标准 OIDC 协议；client ID/secret 等配置从本平台应用接入页领取 |
| **租户管理员** | **仅本平台** | 接入应用/管用户/配策略全在本平台（后台经 Provider 调 Logto）；无需 Logto 后台账号 |
| **运营方** | Logto 后台 | 实例管理，不日常介入 |

```
客户应用 ──── OIDC 认证 ────→ Logto（唯一 IdP）
   │                              ↑
   │ client id/secret 从本平台领取  │（本平台也是 Logto 的一个 OIDC client,认证层平级）
租户管理员 ──管理──→ Authlode ────┘（经 Management API）
```

**本平台不做 IdP、不暴露自己的 OIDC 端点** — 否则等于在业务层重写认证引擎，违背"IAM 层保持薄"原则。

**例外（可选的运行时依赖）**: 仅当选择访问控制执行点 B（平台鉴权 API）或使用 SCIM（T2.3）/租户开放 API（T2.5）等高级能力时，客户系统才在认证之外额外对接本平台。推荐执行点 C（组织 Token）下，客户应用运行时完全不经过本平台 — 平台故障不影响任何已接入应用的登录。

### 3.3 应用接入设计要点（研判新发现）

1. **每租户独立 OIDC client**: 租户"接入自己的应用" = 经 Provider 在 Logto 注册新 Application（实例级）+ 扩展库记录租户-应用映射与策略。不同租户的应用天然是不同 client，无冲突。
2. **✅ 访问控制执行点（2026-09-19 已决，依据产品要求"没注册不能用 / 没资格不能用"）**:
   - **主机制: 组织 Token 强制约定** — 系统接入时，本平台的接入引导默认要求应用采用 organization-scoped token 流程；**非系统成员无法从 IAM 获得该系统的访问凭证** → "没资格用不了"由 IAM 在令牌签发时强制执行，各接入系统无需自研检查（比 Google 的"应用自查"模式更进一步）
   - **细粒度: 策略层** — 系统内角色/资格由 token 中的 org roles + 扩展库租户应用接入配置承载
   - **兜底（可选）: 平台鉴权 API** — 共享应用或特殊场景由应用调用本平台判断访问权
3. **共享应用场景**: 多租户共用同一应用时，策略完全由扩展库配置层承载（Logto 无此概念，已确认）。

### 3.4 模块优先级（研判后重排）

```
批次 1 (P0 地基 — 不变):
  Provider 抽象 + Logto Adapter + OIDC 认证 + Tenant 模型 + tenantId 迁移 + TenantContext

批次 2 (P1 核心价值 — 应用接入提升为同优先级):
  用户管理租户化 + SSO 应用接入(含 3.3.2 访问控制设计) + 前端去 mock + 租户切换器
  ✅ 演示主线闭环: 登录 → 管用户 → 接应用 → 用户 SSO 访问

批次 3 (P2 自管理闭环):
  邀请注册双语义 + 邮件 + 公开注册页 + 审计/设置租户化

批次 4 (P3 增值 + 统一账号体验):
  组织树/批量/通知/统计 + 自助中心(终端用户账号面,路线图内) + (可选)企业 SSO 入站/组织 Token
```

### 3.5 风险与依赖（更新）

| 风险 | 等级 | 缓解 |
|------|------|------|
| 访问控制执行点 | ~~高~~ **已决** | 组织 Token 强制约定 + 策略层（见 3.3.2） |
| 前端 100% mock | 高（批次 2 重头） | 数据层全部重写 |
| 邀请 3 步调用无原子性 | 中 | 幂等设计（先查后建） |
| Prisma Client 技术债 | 中（批次 1 迁移前） | Linux 生成提交 |
| 应用实例级全局 | 低（已设计绕行） | 每租户独立 client + 扩展库映射 |

### 3.6 与原方案差异汇总

| 项 | 原方案（功能模块规划 v1.2） | 研判后（本文） |
|----|------------------------------|----------------|
| 产品定位 | 多租户 IAM 用户管理系统 | **SSO 用户管理平台** |
| 应用接入 | P1 模块之一 | **P1 核心 + 新增访问控制执行点设计** |
| 企业 SSO 入站 / 组织 Token | T1 机会储备 | 优先级上调（批次 4 可选） |
| 终端用户自助中心 | T2 机会储备 | **升为路线图内（批次 4 核心）** — 统一账号体验的必要组成 |
| 架构 / 多租户 / 认证 / 批次 1 | — | **全部不变** |

---

## 四、下一步

1. ~~决策 3.3.2 访问控制执行点~~ ✅ 已决（2026-09-19：组织 Token 强制约定 + 策略层）
2. 出批次 1（P0）详细实施计划：IamProviderInterface 定义 + Tenant 模型 schema + OIDC 认证流程
3. 提交定位变更文档群（README / 总体技术方案 / 功能模块规划 / 本文）

---

**文档版本**: v4.1
**最后更新**: 2026-09-19
**文档状态**: 当前权威总纲
