# Logto 用户中心管理系统 - 总体技术方案 v2.0

## 📋 项目概述

### 项目名称
**Logto 用户中心管理系统** (Logto User Center Management System)

### 项目定位
这是一个**独立的用户中心管理应用**,基于 Logto IAM (身份与访问管理) 作为底层数据源,提供企业级的用户、组织、权限、应用管理功能。

### 核心理念

```
┌─────────────────────────────────────────────────────────┐
│         Logto 用户中心管理系统 (本项目)                  │
│                                                           │
│  - 用户管理界面                                           │
│  - 组织架构管理                                           │
│  - 角色权限配置                                           │
│  - 应用授权管理                                           │
│  - 邀请审批流程                                           │
│  - 审计日志统计                                           │
└─────────────────────────────────────────────────────────┘
                    ↑ 调用 Management API
                    │ 读写核心数据
┌─────────────────────────────────────────────────────────┐
│                    Logto IAM                             │
│              (底层身份认证基础设施)                        │
│                                                           │
│  - 用户身份存储                                           │
│  - 组织架构存储                                           │
│  - 角色权限模型                                           │
│  - OIDC 认证服务                                          │
│  - Management API                                        │
└─────────────────────────────────────────────────────────┘
```

### 数据策略

#### 来自 Logto 的数据 (通过 API,不重复存储)
- ✅ 用户账户信息
- ✅ 用户基本资料
- ✅ 组织架构数据
- ✅ 角色定义
- ✅ 权限定义
- ✅ 用户-组织关系
- ✅ 用户-角色关系
- ✅ 应用注册信息

#### 存储在扩展数据库的数据 (Logto 没有的功能)
- ✅ 邀请记录和状态
- ✅ 审计操作日志
- ✅ 用户扩展信息 (工号、部门等自定义字段)
- ✅ 批量操作任务记录
- ✅ 系统通知记录
- ✅ 应用使用统计
- ✅ 系统配置信息

### 核心特性
- 🔐 基于 Logto OIDC 的身份认证
- 🏢 多层级组织架构可视化管理
- 👥 完整的用户生命周期管理
- 🎭 灵活的 RBAC 权限配置
- 📱 应用授权与访问管理
- 📧 用户邀请与审批流程
- 📊 完整的审计日志系统
- 📈 数据统计与报表

---

## 🏗️ 系统架构设计

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                     用户接入层 (Presentation Layer)              │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │         前端应用 (Nuxt 3 + Vue 3 + TypeScript)            │    │
│  │                                                           │    │
│  │  用户管理  │  组织管理  │  角色权限  │  应用管理  │  邀请系统│    │
│  │  审计日志  │  系统设置  │  数据统计  │  报表分析          │    │
│  │                                                           │    │
│  │              UI 框架: Nuxt UI + Tailwind CSS              │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓ HTTPS/API
┌─────────────────────────────────────────────────────────────────┐
│                   业务逻辑层 (Business Logic Layer)              │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │        后端服务 (NestJS + TypeScript)                     │    │
│  │                                                           │    │
│  │  ┌──────────────────────────────────────────────────┐   │    │
│  │  │        服务层 (Service Layer)                     │   │    │
│  │  │                                                      │   │    │
│  │  │  ┌──────────────────┐    ┌──────────────────┐     │   │    │
│  │  │  │ LogtoDataService │    │ DatabaseService  │     │   │    │
│  │  │  │  (调用 Logto API) │    │  (扩展数据操作)   │     │   │    │
│  │  │  └──────────────────┘    └──────────────────┘     │   │    │
│  │  └──────────────────────────────────────────────────┘   │    │
│  │                                                           │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │    │
│  │  │用户模块  │  │组织模块  │  │角色模块  │  │权限模块  │ │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │    │
│  │                                                           │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │    │
│  │  │应用模块  │  │邀请模块  │  │审计模块  │  │统计模块  │ │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │    │
│  │                                                           │    │
│  │              RESTful API + WebSocket API                  │    │
│  │              Swagger API 文档                              │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
            ↓                                      ↓
┌──────────────────────────────┐    ┌──────────────────────────────┐
│    Logto Management API      │    │   扩展数据库 (MySQL/PG)      │
│                              │    │                              │
│  - 用户 CRUD                  │    │  - 邀请记录                  │
│  - 组织 CRUD                  │    │  - 审计日志                  │
│  - 角色 CRUD                  │    │  - 用户扩展信息              │
│  - 权限 CRUD                  │    │  - 批量操作记录              │
│  - 应用 CRUD                  │    │  - 系统通知                  │
│  - 关系管理                   │    │  - 统计缓存                  │
└──────────────────────────────┘    └──────────────────────────────┘
```

### 数据流设计

#### 读取数据流程
```
前端请求 → Controller → Service
                              ↓
                    ┌─────────────────────┐
                    │  判断数据来源        │
                    └─────────────────────┘
                         ↓         ↓
              [Logto 数据]  [扩展数据]
                    ↓             ↓
          调用 Logto API    查询数据库
                    ↓             ↓
                    └──────┬───────┘
                           ↓
                      合并数据返回
```

#### 写入数据流程
```
前端请求 → Controller → Service
                              ↓
                    ┌─────────────────────┐
                    │  判断数据类型        │
                    └─────────────────────┘
                         ↓         ↓
              [Logto 核心]  [扩展功能]
                    ↓             ↓
          调用 Logto API    写入数据库
                    ↓             ↓
              更新 Logto     记录到数据库
```

---

## 🛠️ 技术栈选型

### 前端技术栈

| 技术领域 | 技术选型 | 版本 | 说明 |
|---------|---------|------|------|
| **框架** | Nuxt 3 | 3.x | 基于 Vue 3 的全栈框架 |
| **语言** | TypeScript | 5.x | 类型安全 |
| **UI 组件库** | Nuxt UI | 最新 | 现代化组件库 |
| **样式框架** | Tailwind CSS | 3.x | 原子化 CSS |
| **状态管理** | Pinia | 2.x | 官方推荐状态管理 |
| **HTTP 客户端** | ofetch | 内置 | Nuxt 内置 |
| **表单验证** | VeeValidate | 最新 | 表单验证 |
| **日期处理** | Day.js | 最新 | 轻量日期库 |
| **图标库** | Iconify | 最新 | 统一图标框架 |

### 后端技术栈

| 技术领域 | 技术选型 | 版本 | 说明 |
|---------|---------|------|------|
| **框架** | NestJS | 10.x | 企业级 Node.js 框架 |
| **语言** | TypeScript | 5.x | 类型安全 |
| **ORM** | Prisma | 5.x | 类型安全 ORM (仅扩展数据) |
| **HTTP 客户端** | Axios / Fetch | 最新 | 调用 Logto API |
| **验证** | class-validator | 最新 | 装饰器验证 |
| **配置管理** | @nestjs/config | 最新 | 配置管理 |
| **API 文档** | Swagger/OpenAPI | 最新 | 自动生成文档 |
| **日志** | Winston | 最新 | 日志管理 |

### 数据库技术栈

| 数据类型 | 技术选型 | 说明 |
|---------|---------|------|
| **扩展数据库** | MySQL 8.0+ / PostgreSQL 15+ | 存储扩展数据 |
| **缓存** | Redis 7+ | 缓存和会话 (可选) |

**注意**:
- 开发阶段: 连接已有的数据库实例,不使用 Docker
- 生产阶段: 使用 Docker 容器化部署

---

## 📐 系统功能模块

### 模块划分原则

#### A 类模块: 操作 Logto 数据 (通过 Management API)
- 用户管理
- 组织管理
- 角色管理
- 权限管理
- 应用管理

#### B 类模块: 扩展功能 (存储到扩展数据库)
- 邀请系统
- 审计日志
- 用户扩展信息
- 批量操作
- 系统通知
- 数据统计

---

### 模块一: 用户管理 (Logto 数据)

#### 功能清单
1. **用户列表查询**
   - 分页查询 (调用 Logto API)
   - 多条件搜索
   - 状态筛选

2. **用户详情查看**
   - 基本信息来自 Logto
   - 扩展信息来自数据库
   - 合并展示

3. **用户信息编辑**
   - 更新 Logto 用户信息
   - 更新扩展数据库信息

4. **用户组织关系**
   - 调用 Logto API 管理关系
   - 查看用户所属组织

5. **用户角色分配**
   - 调用 Logto API 分配角色
   - 查看用户权限

6. **用户状态管理**
   - 启用/禁用 (Logto API)
   - 锁定/解锁

#### 技术实现
```typescript
// 用户服务示例
@Injectable()
export class UsersService {
  constructor(
    private readonly logtoApiService: LogtoApiService,
    private readonly userExtendRepository: UserExtendRepository,
  ) {}

  async findAll(query: UserQueryDto) {
    // 1. 从 Logto API 获取用户列表
    const logtoUsers = await this.logtoApiService.getUsers(query);

    // 2. 获取扩展信息
    const userIds = logtoUsers.data.map(u => u.id);
    const extends = await this.userExtendRepository.findByIds(userIds);

    // 3. 合并数据
    return logtoUsers.data.map(user => ({
      ...user,
      extend: extends.find(e => e.userId === user.id),
    }));
  }

  async update(userId: string, data: UpdateUserDto) {
    // 1. 更新 Logto 用户信息
    if (data.name || data.email) {
      await this.logtoApiService.updateUser(userId, {
        name: data.name,
        email: data.email,
      });
    }

    // 2. 更新扩展信息
    if (data.department || data.position) {
      await this.userExtendRepository.upsert(userId, {
        userId,
        department: data.department,
        position: data.position,
      });
    }
  }
}
```

---

### 模块二: 组织管理 (Logto 数据)

#### 功能清单
1. **组织树形结构**
   - 从 Logto 获取组织数据
   - 构建树形结构
   - 展示层级关系

2. **组织信息管理**
   - 创建组织 (Logto API)
   - 更新组织 (Logto API)
   - 删除组织 (Logto API)

3. **组织成员管理**
   - 添加成员 (Logto API)
   - 移除成员 (Logto API)
   - 设置管理员

---

### 模块三: 角色与权限管理 (Logto 数据)

#### 功能清单
1. **角色管理**
   - 角色列表 (Logto API)
   - 创建角色 (Logto API)
   - 编辑角色 (Logto API)
   - 删除角色 (Logto API)

2. **权限配置**
   - 权限列表 (Logto API)
   - 分配权限 (Logto API)
   - 查看角色权限

3. **用户角色分配**
   - 分配角色 (Logto API)
   - 撤销角色 (Logto API)
   - 查看用户权限

---

### 模块四: 应用管理 (Logto 数据)

#### 功能清单
1. **应用注册管理**
   - 应用列表 (Logto API)
   - 注册应用 (Logto API)
   - 编辑应用 (Logto API)

2. **应用授权管理**
   - 查看授权 (Logto API)
   - 授权用户 (Logto API)
   - 撤销授权 (Logto API)

---

### 模块五: 邀请系统 (扩展数据库)

#### 功能清单
1. **创建邀请**
   - 生成邀请 Token
   - 设置邀请角色
   - 设置邀请组织
   - 发送邀请邮件

2. **邀请管理**
   - 邀请列表 (数据库)
   - 取消邀请
   - 重发邀请
   - 过期清理

3. **邀请接受**
   - 验证邀请 Token
   - 创建 Logto 用户
   - 分配角色和组织
   - 更新邀请状态

#### 数据模型
```prisma
model UserInvitation {
  id              String              @id @default(cuid())
  email           String
  token           String              @unique
  organizationId  String?             // Logto Organization ID
  roleIds         String              // JSON: Logto Role IDs
  status          InvitationStatus    @default(PENDING)
  invitedBy       String              // Logto User ID
  message         String?
  acceptedBy      String?
  expiresAt       DateTime
  acceptedAt      DateTime?
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt

  @@index([email])
  @@index([token])
  @@index([status])
  @@map("user_invitations")
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  EXPIRED
  CANCELLED
}
```

---

### 模块六: 审计日志 (扩展数据库)

#### 功能清单
1. **操作日志记录**
   - 自动记录所有管理操作
   - 记录操作人和时间
   - 记录操作详情

2. **日志查询**
   - 按用户查询
   - 按操作类型查询
   - 按时间范围查询
   - 组合条件查询

3. **日志导出**
   - 导出为 CSV
   - 导出为 JSON

#### 数据模型
```prisma
model AuditLog {
  id              String              @id @default(cuid())
  userId          String?             // Logto User ID
  userName        String?             // 冗余字段,方便查询
  action          String              // user.create, org.update, etc.
  resource        String              // user, organization, role, etc.
  resourceId      String?             // Logto Resource ID
  resourceName    String?             // 冗余字段
  details         String?             // JSON: 操作详情
  ipAddress       String?
  userAgent       String?
  success         Boolean             @default(true)
  errorMessage    String?
  executionTime   Int?                // 毫秒
  createdAt       DateTime            @default(now())

  @@index([userId])
  @@index([action])
  @@index([resource])
  @@index([createdAt])
  @@map("audit_logs")
}
```

---

### 模块七: 用户扩展信息 (扩展数据库)

#### 功能清单
1. **扩展信息管理**
   - 工号管理
   - 部门信息
   - 职位信息
   - 直属上级
   - 自定义字段

2. **信息查询**
   - 查看用户扩展信息
   - 按部门查询
   - 按工号查询

#### 数据模型
```prisma
model UserExtend {
  userId                  String          @id // Logto User ID
  employeeId              String?         @unique
  department              String?
  position                String?
  managerId               String?         // Logto User ID
  workPhone               String?
  address                 String?
  bio                     String?
  customFields            String?         // JSON
  preferences            String?         // JSON
  lastLoginIp             String?
  lastLoginLocation       String?
  profileCompleted        Boolean         @default(false)
  notes                   String?
  internalTags            String?         // JSON
  riskLevel               String          @default("low")
  createdAt              DateTime        @default(now())
  updatedAt              DateTime        @updatedAt

  @@index([department])
  @@index([managerId])
  @@map("user_extends")
}
```

---

### 模块八: 批量操作 (扩展数据库)

#### 功能清单
1. **批量导入**
   - 用户导入
   - 组织导入
   - 角色导入

2. **批量更新**
   - 批量更新用户
   - 批量分配角色

3. **操作记录**
   - 任务状态追踪
   - 错误记录
   - 结果统计

#### 数据模型
```prisma
model BatchOperation {
  id              String              @id @default(cuid())
  type            OperationType
  status          OperationStatus     @default(PENDING)
  total           Int                 @default(0)
  processed       Int                 @default(0)
  success         Int                 @default(0)
  failed          Int                 @default(0)
  fileUrl         String?
  errorFileUrl    String?
  resultSummary   String?             // JSON
  createdBy       String              // Logto User ID
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt
  startedAt       DateTime?
  completedAt     DateTime?

  @@index([status])
  @@index([createdBy])
  @@map("batch_operations")
}

enum OperationType {
  USER_IMPORT
  USER_EXPORT
  USER_BULK_UPDATE
  ORGANIZATION_IMPORT
  ROLE_IMPORT
}

enum OperationStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  CANCELLED
}
```

---

## 🌐 Logto Management API 集成

### API 封装服务

```typescript
// src/modules/logto/logto-api.service.ts
@Injectable()
export class LogtoApiService {
  private readonly apiEndpoint: string;
  private readonly apiKey: string;

  constructor(private readonly httpService: HttpService) {
    this.apiEndpoint = process.env.LOGTO_MANAGEMENT_API_ENDPOINT;
    this.apiKey = process.env.LOGTO_MANAGEMENT_API_KEY;
  }

  private get headers() {
    return {
      'Authorization': `Bearer ${this.apiKey}`,
      'Content-Type': 'application/json',
    };
  }

  // ============================================
  // 用户 API
  // ============================================

  async getUsers(params: {
    page?: number;
    pageSize?: number;
    search?: string;
  }) {
    const response = await this.httpService.axiosRef.get(
      `${this.apiEndpoint}/users`,
      { params, headers: this.headers }
    );
    return response.data;
  }

  async getUserById(userId: string) {
    const response = await this.httpService.axiosRef.get(
      `${this.apiEndpoint}/users/${userId}`,
      { headers: this.headers }
    );
    return response.data;
  }

  async createUser(data: CreateUserDto) {
    const response = await this.httpService.axiosRef.post(
      `${this.apiEndpoint}/users`,
      data,
      { headers: this.headers }
    );
    return response.data;
  }

  async updateUser(userId: string, data: UpdateUserDto) {
    const response = await this.httpService.axiosRef.patch(
      `${this.apiEndpoint}/users/${userId}`,
      data,
      { headers: this.headers }
    );
    return response.data;
  }

  async deleteUser(userId: string) {
    await this.httpService.axiosRef.delete(
      `${this.apiEndpoint}/users/${userId}`,
      { headers: this.headers }
    );
  }

  // ============================================
  // 组织 API
  // ============================================

  async getOrganizations() {
    const response = await this.httpService.axiosRef.get(
      `${this.apiEndpoint}/organizations`,
      { headers: this.headers }
    );
    return response.data;
  }

  async getOrganizationTree() {
    const orgs = await this.getOrganizations();
    return this.buildTree(orgs);
  }

  async createOrganization(data: CreateOrganizationDto) {
    const response = await this.httpService.axiosRef.post(
      `${this.apiEndpoint}/organizations`,
      data,
      { headers: this.headers }
    );
    return response.data;
  }

  async addOrganizationUser(organizationId: string, userId: string) {
    await this.httpService.axiosRef.post(
      `${this.apiEndpoint}/organizations/${organizationId}/users/${userId}`,
      {},
      { headers: this.headers }
    );
  }

  // ============================================
  // 角色 API
  // ============================================

  async getRoles() {
    const response = await this.httpService.axiosRef.get(
      `${this.apiEndpoint}/roles`,
      { headers: this.headers }
    );
    return response.data;
  }

  async assignRoleToUser(userId: string, roleId: string) {
    await this.httpService.axiosRef.post(
      `${this.apiEndpoint}/users/${userId}/roles`,
      { roleIds: [roleId] },
      { headers: this.headers }
    );
  }

  // ============================================
  // 应用 API
  // ============================================

  async getApplications() {
    const response = await this.httpService.axiosRef.get(
      `${this.apiEndpoint}/applications`,
      { headers: this.headers }
    );
    return response.data;
  }

  // ... 其他 API 方法
}
```

---

## 🔒 安全设计

### 认证与授权

1. **身份认证**
   - 使用 Logto OIDC 进行登录
   - JWT Token 验证
   - Token 刷新机制

2. **权限控制**
   - 基于 Logto 的角色权限
   - API 级别权限检查
   - 数据权限过滤

3. **审计追踪**
   - 记录所有管理操作
   - 敏感操作详细记录
   - 异常行为告警

---

## 🚀 部署架构

### 开发环境
```
- 前端: Nuxt 3 (本地运行)
- 后端: NestJS (本地运行)
- 数据库: 连接已有 MySQL/PostgreSQL 实例
- Redis: 连接已有 Redis 实例 (可选)
- Logto: 连接已有的 Logto 实例
```

### 生产环境 (Docker 部署)
```yaml
services:
  frontend:
    image: logto-user-center-frontend:latest
    ports:
      - "3000:3000"

  backend:
    image: logto-user-center-backend:latest
    environment:
      - DATABASE_HOST=postgres
      - REDIS_HOST=redis
    ports:
      - "3001:3001"

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=logto_user_center
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
```

---

## 📅 开发计划

### 阶段 0: 环境准备 (1-2天)
- 安装开发工具
- 配置数据库连接
- 配置 Logto 连接
- 创建项目结构

### 阶段 1: 项目初始化 (2-3天)
- 初始化后端项目
- 初始化前端项目
- 配置开发工具
- 创建基础模块

### 阶段 2: Logto API 集成 (3-4天)
- 实现 Logto API 封装
- 实现用户数据读取
- 实现组织数据读取
- 测试 API 调用

### 阶段 3: 扩展数据库设计 (2-3天)
- 设计扩展数据模型
- 创建 Prisma Schema
- 实现数据迁移
- 创建基础 Repository

### 阶段 4: 核心功能开发 (5-7天)
- 用户管理模块
- 组织管理模块
- 角色权限模块
- 应用管理模块

### 阶段 5: 扩展功能开发 (4-5天)
- 邀请系统
- 审计日志
- 用户扩展信息
- 批量操作

### 阶段 6: 前端界面开发 (7-10天)
- 用户管理页面
- 组织管理页面
- 角色权限页面
- 邀请管理页面

### 阶段 7: 测试与优化 (3-5天)
- 功能测试
- 性能优化
- 安全加固

### 阶段 8: Docker 部署 (2-3天)
- 创建 Dockerfile
- 配置 Docker Compose
- 部署测试

---

## 🎯 成功标准

### 功能指标
- ✅ 所有 Logto 核心数据可正常管理
- ✅ 扩展功能正常运行
- ✅ 审计日志完整记录
- ✅ 权限控制准确

### 性能指标
- ✅ API 响应时间 < 300ms (P95)
- ✅ 页面加载时间 < 2s
- ✅ 支持 500+ 并发用户

---

**文档版本**: v2.0
**最后更新**: 2025-01-28
**文档状态**: 已更新
